---
name: meeting-report
description: 주기적 미팅용 보고자료 HTML 생성. 사용자가 "자료 제작"이라고 하면 오늘 작업의 하루치 요약 HTML을, "총자료 제작"이라고 하면 일주일치 종합(total) HTML을 현재 디렉토리의 reports/ 아래 주간 폴더에 생성. Triggers on "자료 제작" (daily report) and "총자료 제작" (weekly total report); writes self-contained HTML into reports/<YYYYMMDD_YYYYMMDD>/.
---

# meeting-report — 미팅 보고자료 생성 스킬

연구 프로젝트의 주간 미팅(예: 매주 수요일 교수님 1:1)용 보고자료를 만든다.
두 가지 모드:

- **"자료 제작"** (당일): 오늘 세션에서 한 작업 중 중요한 것만 추려 하루치 HTML
- **"총자료 제작"** (미팅 전날): 그 주의 일일 리포트들을 종합해 특히 중요했던
  내용만 선별한 주간 HTML

## 1. 폴더/파일 규칙

모든 리포트는 **Claude Code를 실행한 현재 디렉토리** 기준 `reports/` 아래:

```
reports/
└── 20260701_20260707/     ← 주간 폴더 (시작일_종료일, 7일 창)
    ├── 20260701.html      ← 일일 리포트 (당일 날짜)
    ├── 20260703.html
    └── total.html         ← 주간 종합 (총자료)
```

**주간 폴더 결정 알고리즘** (자료 제작 시 매번 수행):
1. `reports/` 안의 `YYYYMMDD_YYYYMMDD` 폴더 중 가장 최신을 찾는다.
2. 오늘이 그 폴더의 날짜 범위 안이고 `total.html`이 **없으면** → 그 폴더 사용.
3. 아니면 (범위 지남 / total 이미 생성 / 폴더 없음) → 새 폴더
   `<오늘>_<오늘+6일>` 생성. (직전 total 다음날부터 새 주가 시작되는 효과)
4. 총자료 제작은 항상 현재 활성 주간 폴더에 `total.html`로. 이미 있으면
   덮어쓰기 전에 사용자에게 확인.

## 2. 내용 수집

### 일일 ("자료 제작")
우선순위 순서로 수집:
1. **현재 세션 대화** — 오늘 실제로 한 일, 나온 수치, 내린 결정 (1차 소스)
2. 프로젝트 일지 파일 (`LOG.md`, `*.log.md`, `journal*` 등이 있으면 오늘 날짜 항목)
3. `git log --since=midnight --oneline` (git repo인 경우)
4. 오늘 수정된 결과 파일 (`find . -newermt today -name "*.json" -o -name "*.png"` 등,
   과하게 뒤지지 말고 세션에서 언급된 것 위주)

**선별 기준** — 전부 넣지 않는다. 다음에 해당하는 것만:
- 결론이 바뀌거나 새로 선 것 (신기록, 판정, 가설 확정/기각)
- 미팅에서 질문받을 수치 (표로)
- 실패/막힌 것도 정직하게 (원인 진단이 있으면 함께)
- 다음 단계

### 주간 ("총자료 제작")
1. **활성 주간 폴더의 일일 HTML들이 1차 소스** — 다른 세션에서 실행해도
   일주일치가 복원되도록. 각 일일 HTML을 읽고 핵심을 추출.
2. 현재 세션 컨텍스트로 보강 (당일 작업이 아직 일일 리포트에 없으면 포함).
3. **"특히 중요했던 것"만** — 미팅에서 5–10분에 설명 가능한 분량으로 압축.
   일일 리포트의 나열이 아니라 주 단위 스토리로 재구성:
   무엇이 진전됐나 → 근거 수치 → 남은 리스크 → 다음 주 계획 → 논의 필요 사항.

## 3. HTML 규격 — "교수님급" 디자인 (self-contained 필수)

**★ 이 스킬 폴더의 `example_builder.py`가 표준이다** — 실제 리포트에서 검증된
python 빌더 (CSS + 레이아웃 + base64 임베드 전부 포함). 새 리포트는 이 빌더를
복사해 내용(hero/섹션/표/그림)만 교체하는 방식으로 만들 것. 미니멀 템플릿으로
새로 짜지 말 것 (한 번 그렇게 했다가 품질 하락 지적받음, 2026-07-04).

필수 구성 요소 (example_builder.py에 다 있음):
- **고정 상단 네비게이션 바** (섹션 앵커 링크) + **스크롤 진행바**
- **Hero 섹션**: kick(작은 라벨) → 큰 제목(핵심 결론, `<em>` 강조) → lead 문단
  → **pill 뱃지** (오늘의 성과 3-5개) → meta 라인
- **번호 섹션들**: kick → `h2`(번호 칩) → **take**(섹션 한줄 결론 카드) →
  figure 카드(그림+캡션) / 표 / bullet — 그림과 표를 나란히 놓을 땐 `.duo` 그리드
- **note 박스**: 주의사항은 `.note.o`(주황), 실패/틀린 예측은 `.note.c`(적색) —
  정직 보고를 시각적으로 구분
- **표**: `.s` 클래스 (둥근 모서리 카드, 숫자 우측정렬 `.n`, 승/기록은 `.keep`)
- **print CSS**: 섹션별 페이지 분할, 네비 숨김 — 그대로 인쇄해서 미팅에 들고 갈 수 있게
- **이미지**: base64 data-URI 임베드 (외부 리소스 0). 결과 그림뿐 아니라
  **구조 렌더링** (ASE `write('x.png', atoms, rotation=...)`)도 적극 포함 —
  구조 이미지가 있으면 미팅 설득력이 크게 오름. 이미지당 ~500KB 이하
- **구조** (일일): hero(TL;DR) → 핵심 결과 섹션 3-6개 → 이슈·정직보고 → 다음 단계
- **구조** (주간 total): 위에 더해 **미팅 논의 포인트** 섹션 (지도교수 결정 필요 항목)
- 언어는 사용자가 쓰는 언어 (기본 한국어), 기술용어는 원어 유지

## 4. 작성 태도

- 본론만. 미사여구·과장 금지. "~했음/~로 판정" 톤
- 모든 주장 옆에 근거 수치 (show-me)
- 부정적 결과도 보고 — 원인 진단이 붙으면 그게 성과
- 리포트 생성 후 사용자에게 파일 경로와 한 줄 요약만 알려주면 됨
